const defaultState = {
  entries: [],
  status: 'ready'
}

const reducer = (state = { ...defaultState }, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
